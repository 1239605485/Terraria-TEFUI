# TEFUI v0.2.0

本版修复 Android UI 坐标与拖动。

- 启动键和菜单默认放回左侧安全位置，避开中部坐标导致的 UI 消失。
- 手指按住启动键后移动即可拖动；轻触仍可打开/关闭菜单。
- 保留原生 `GUIStringButton` 点击与 `GUISlider` 滑块输入。

上传整个工程到 GitHub 后运行 `Build TEFUI Android ARM64` 即可编译。
